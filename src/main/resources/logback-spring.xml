<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="JSON-FORMATTER-STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.contrib.json.classic.JsonLayout">
            <jsonFormatter class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter">
                <prettyPrint>true</prettyPrint>
            </jsonFormatter>
            <timestampFormat>yyyy-MM-dd HH:mm:ss</timestampFormat>
        </layout>

    </appender>

    <appender name="JSON-STASH-FORMATTER-STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">

                <jsonGeneratorDecorator class="br.com.appday.logback.JsonGeneratorDecoratorConfig"/>

            <providers>
                <arguments/>
                <timestamp>
                    <pattern>yyyy-MM-dd HH:mm:ss</pattern>
                </timestamp>
                <nestedField>
                    <fieldName>@fields</fieldName>
                    <providers>
                        <logLevel/>
                    </providers>
                </nestedField>
            </providers>
        </encoder>
    </appender>

    <logger name="br.com.appday" level="DEBUG">
        <appender-ref ref="JSON-FORMATTER-STDOUT" />
        <appender-ref ref="JSON-STASH-FORMATTER-STDOUT" />
    </logger>

</configuration>